import os

print(os.path)  # gives the path
# os.mkdir("test-dir")  # makes a directory called test, will throw error if directory already exists

# os.mknod("test.md")  # makes a file called test.md, will throw error if file exists

print(os.environ['PS1'])  # (venv) - virtual environment

