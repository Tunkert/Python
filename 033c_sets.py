# create a set
# my_set = set(1, 2, 3) # this will throw an error

my_set = set([1, 2, 3, 4, 5])   # we can do this

print(my_set)

my_other_set = set([1, 2, 2, 3, 3, 4, 5])  # or even this ...

print(my_other_set)

my_other_other_set = ((1, 2, 3, 4, 5))  # or this ... experiment

print(my_other_other_set)
